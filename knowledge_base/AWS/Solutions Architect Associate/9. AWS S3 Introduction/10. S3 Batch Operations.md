- Allows to perform bulk operations on **existing** S3 objects with a single request
	- Modify object metadata and properties
	- Copy objects between S3 buckets
	- Encrypt the un-encrypted objects in the bucket
	- Modify ACLs tags
	- Restore objects from S3 Glacier
	- Invoke Lambda function to perform custom action on each object in bucket using S3 batch operation
- A Batch job consists of a list of objects, the actions to perform, and optional parameters

- **Why S3 batch over scripting it ourselves?**
	- S3 Batch Operations 
		- manages retries, 
		- tracks progress, 
		- sends completion notifications, 
		- generate reports etc
- Can use S3 Inventory to get object list and use Athena to query and filter our objects
	- can use this architecture to find all unencrypted objects using S3 inventory
- ![[Pasted image 20260520005918.png]]
- E.g. find all unencrypted objects using S3 Inventory and encrypt them all at once using S3 batch operation